<!DOCTYPE html>
<html lang="en" xml:lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "SknServices" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'SknServices Authentication and Authorization demo using rails 4.2' %>" />
    <meta name="author" content="systems@skoona.net"/>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => false %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>

    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon', :href => image_path('favicon.ico') %>

    <%= yield(:head) %>

  </head>

  <body class="language-ruby preload" data-controller-name="<%= controller_name %>" data-controller-action="<%= action_name %>" data-relative-path="<%= Rails.application.config.relative_url_root %>" >  <%# Identifies each page so JS can act when wanted %>
    <header>
      <%= render( 'layouts/navigation') %>
    </header>

    <main role="main" class="container">
        <%= render 'layouts/messages' %>
        <%= yield %>
    </main>

    <%= render( 'layouts/footer') %>
  </body>
</html>
