<!DOCTYPE html>
<html lang="en" xml:lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= content_for?(:title) ? yield(:title) : "Skoona Services" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Skoona Services Authentication and Authorization demo using rails 4." %>">
    <meta name="author" content="systems@skoona.net"/>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>

    <%= yield(:head) %>

    <link rel="shortcut icon" href="<%= image_path("favicon.ico") %>"/>
  </head>

  <body data-controller-name="<%= controller_name %>" data-controller-action="<%= action_name %>">  <%# Identifies each page so JS can act when wanted %>

      <main role="main">
       <%= render( 'layouts/navigation') %>

       <%= render 'layouts/messages' %>

       <%= yield %>
    </main>

  </body>
</html>
